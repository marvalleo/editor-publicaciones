# Fase 1 · Tarea 1.2 — Modelo de datos Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Crear `dcpub/models.py` con `Layer` y subclases (`PhotoLayer`, `LogoLayer`, `TextLayer`, `BoxLayer`), `Slide`, `Project`, serialización `to_dict`/`from_dict`, y una factory `crear_proyecto_por_defecto()` que reproduce exactamente el proyecto por defecto de v2.0 (foto + logo + título + subtítulo + caja) sobre el nuevo modelo.

**Architecture:** Dataclasses de Python puras (sin tkinter, sin Pillow) para que el modelo sea testeable de forma aislada y trivialmente serializable. `Layer.to_dict()` usa `dataclasses.asdict()` (gratis, recursivo, sin boilerplate por subclase). La reconstrucción (`from_dict`) sí necesita lógica propia porque hay que decidir qué subclase de `Layer` instanciar a partir del campo `"type"` guardado — eso lo resuelve un dispatcher `layer_from_dict()` con un registro `LAYER_CLASSES`. `Slide`/`Project` reconstruyen sus listas anidadas llamando a eso. Esta tarea **no** conecta el modelo con `render.py` ni `app.py` — sigue el alcance textual del plan maestro ("crea `models.py`... factory"); la integración real llega en tareas posteriores (1.3/1.4) cuando `CanvasEditor` necesite manipular objetos `Layer` de verdad.

**Tech Stack:** Python 3 stdlib únicamente (`dataclasses`, `uuid`) — sin nuevas dependencias. Tests con `unittest` (stdlib).

## Global Constraints

- Solo módulos estándar de Python (sin nuevas dependencias; ni siquiera Pillow es necesario en `models.py`).
- Posiciones/tamaños de capa en fracciones (0.0–1.0) del lienzo — ya es la convención de v2.0/Tarea 1.1, se mantiene.
- Todo debe ser serializable a dict/JSON vía `to_dict()`/`from_dict()`.
- `TextLayer` y `BoxLayer` exponen **solo contenido y tamaño** en Fase 1 (sin fuente/color/estilos ricos — eso es Fase 4, per CLAUDE.md sección 7.1.5).
- `PhotoLayer` incluye `fit`/`zoom`/`offset_x`/`offset_y` (ya son de Fase 1, aunque `render.py` recién los use en la Tarea 1.3); **no** incluye `adjust{}`/`overlay{}` (Fase 3).
- `Project.palette` captura los colores **actuales de v2.0** (verde `#8DC26F`), no la paleta nueva de marca — ese cambio de paleta es explícitamente Fase 5 (`presets/palette.py`), no corresponde adelantarlo aquí.
- `Project.shared` existe como campo (para estabilidad del formato JSON) pero queda vacío `{}` — se puebla en Fase 2.
- Nombres y comentarios en español; sin pseudocódigo ni partes "por completar".
- Esta tarea NO modifica `dcpub/render.py`, `dcpub/app.py` ni `generar_publicacion.py` — es un módulo nuevo y aislado.
- Commit al cerrar cada tarea (task) de este plan.

---

### Task 1: Layer base + subclases + serialización + dispatcher

**Files:**
- Create: `tests/test_models_layer.py`
- Create: `dcpub/models.py`

**Interfaces:**
- Produces: `dcpub.models.Layer` (dataclass base: `id`, `name`, `type`, `visible`, `locked`, `z`, `x`, `y`, `w`, `h`, `rotation`, `opacity`, método `to_dict() -> dict`). Subclases `PhotoLayer(Layer)` (`src`, `fit="cover"`, `zoom=1.0`, `offset_x=0.0`, `offset_y=0.0`), `LogoLayer(Layer)` (`src`, `keep_ratio=True`), `TextLayer(Layer)` (`text=""`, `role="free"`, `size=0.05`), `BoxLayer(Layer)` (`text=""`, `icon="ninguno"`, `size=0.033`). También `LAYER_CLASSES: dict[str, type[Layer]]` y `layer_from_dict(data: dict) -> Layer`.

- [ ] **Step 1: Escribir el test que falla — `tests/test_models_layer.py`**

```python
"""Tests de Layer y sus subclases (dcpub.models)."""

import unittest

from dcpub.models import (
    Layer, PhotoLayer, LogoLayer, TextLayer, BoxLayer, layer_from_dict,
)


class TestLayerSubclasses(unittest.TestCase):
    def test_layer_has_autogenerated_unique_id(self):
        a = Layer()
        b = Layer()
        self.assertTrue(a.id)
        self.assertNotEqual(a.id, b.id)

    def test_photo_layer_defaults(self):
        p = PhotoLayer(src="foto.jpg")
        self.assertEqual(p.type, "photo")
        self.assertEqual(p.fit, "cover")
        self.assertEqual(p.zoom, 1.0)
        self.assertEqual(p.offset_x, 0.0)
        self.assertEqual(p.offset_y, 0.0)

    def test_logo_layer_defaults(self):
        logo = LogoLayer(src="logo.png")
        self.assertEqual(logo.type, "logo")
        self.assertTrue(logo.keep_ratio)

    def test_text_layer_defaults(self):
        t = TextLayer(text="Hola", role="title", size=0.08)
        self.assertEqual(t.type, "text")
        self.assertEqual(t.text, "Hola")
        self.assertEqual(t.role, "title")
        self.assertEqual(t.size, 0.08)

    def test_box_layer_defaults(self):
        b = BoxLayer(text="desc", icon="planta", size=0.03)
        self.assertEqual(b.type, "box")
        self.assertEqual(b.icon, "planta")

    def test_to_dict_includes_all_fields(self):
        t = TextLayer(text="Hola", role="title")
        d = t.to_dict()
        self.assertEqual(d["text"], "Hola")
        self.assertEqual(d["role"], "title")
        self.assertIn("id", d)
        self.assertIn("x", d)


class TestLayerFromDict(unittest.TestCase):
    def test_round_trip_preserves_type_and_fields(self):
        original = PhotoLayer(name="Foto", src="foto.jpg", zoom=1.5)
        restored = layer_from_dict(original.to_dict())
        self.assertIsInstance(restored, PhotoLayer)
        self.assertEqual(restored.src, "foto.jpg")
        self.assertEqual(restored.zoom, 1.5)
        self.assertEqual(restored.id, original.id)

    def test_round_trip_each_subclass(self):
        layers = [
            PhotoLayer(src="a.jpg"),
            LogoLayer(src="logo.png"),
            TextLayer(text="T", role="subtitle"),
            BoxLayer(text="D", icon="corazón"),
        ]
        for layer in layers:
            restored = layer_from_dict(layer.to_dict())
            self.assertIs(type(restored), type(layer))
            self.assertEqual(restored.to_dict(), layer.to_dict())


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Ejecutar el test y confirmar que falla**

Run: `python -m unittest tests.test_models_layer -v`
Expected: FAIL / ERROR — `ModuleNotFoundError: No module named 'dcpub.models'`

- [ ] **Step 3: Implementar `dcpub/models.py`**

```python
"""Modelo de datos: capas (Layer y subclases) con (de)serialización a dict."""

from dataclasses import dataclass, field, asdict
import uuid


def _short_id() -> str:
    return uuid.uuid4().hex[:8]


@dataclass
class Layer:
    id: str = field(default_factory=_short_id)
    name: str = ""
    type: str = "layer"
    visible: bool = True
    locked: bool = False
    z: int = 0
    x: float = 0.0
    y: float = 0.0
    w: float = 0.0
    h: float = 0.0
    rotation: float = 0.0
    opacity: float = 1.0

    def to_dict(self) -> dict:
        return asdict(self)


@dataclass
class PhotoLayer(Layer):
    type: str = "photo"
    src: str = ""
    fit: str = "cover"
    zoom: float = 1.0
    offset_x: float = 0.0
    offset_y: float = 0.0


@dataclass
class LogoLayer(Layer):
    type: str = "logo"
    src: str = ""
    keep_ratio: bool = True


@dataclass
class TextLayer(Layer):
    type: str = "text"
    text: str = ""
    role: str = "free"
    size: float = 0.05


@dataclass
class BoxLayer(Layer):
    type: str = "box"
    text: str = ""
    icon: str = "ninguno"
    size: float = 0.033


LAYER_CLASSES = {
    "photo": PhotoLayer,
    "logo": LogoLayer,
    "text": TextLayer,
    "box": BoxLayer,
}


def layer_from_dict(data: dict) -> Layer:
    """Reconstruye la subclase de Layer correcta según su campo "type"."""
    cls = LAYER_CLASSES[data["type"]]
    return cls(**data)
```

- [ ] **Step 4: Ejecutar el test y confirmar que pasa**

Run: `python -m unittest tests.test_models_layer -v`
Expected: `OK` (7 tests pasan)

- [ ] **Step 5: Commit**

```bash
git add tests/test_models_layer.py dcpub/models.py
git commit -m "Crear Layer y subclases en dcpub/models.py con serializacion a dict"
```

---

### Task 2: Slide + Project

**Files:**
- Create: `tests/test_models_project.py`
- Modify: `dcpub/models.py` (agregar `Slide` y `Project` al final del archivo existente de Task 1; también agregar el import de `.constants` al inicio)

**Interfaces:**
- Consumes: `dcpub.models.{Layer, TextLayer, PhotoLayer, layer_from_dict}` (Task 1). `dcpub.constants.{VERDE, BLANCO, BOX_COLOR}` (Tarea 1.1, ya existen).
- Produces: `dcpub.models.Slide` (dataclass: `format` dict `{name,w,h}` default `feed_4x5` 1080×1350, `layout_tag: str|None`, `layers: list[Layer]`, métodos `to_dict()`/`from_dict(data)` classmethod). `dcpub.models.Project` (dataclass: `version=1`, `name`, `default_format`, `palette` dict con los colores de v2.0, `shared={}`, `slides: list[Slide]`, métodos `to_dict()`/`from_dict(data)` classmethod).

- [ ] **Step 1: Escribir el test que falla — `tests/test_models_project.py`**

```python
"""Tests de Slide y Project (dcpub.models)."""

import unittest

from dcpub.models import Slide, Project, TextLayer, PhotoLayer


class TestSlide(unittest.TestCase):
    def test_default_format_is_feed_4x5(self):
        slide = Slide()
        self.assertEqual(slide.format, {"name": "feed_4x5", "w": 1080, "h": 1350})

    def test_round_trip_preserves_layers(self):
        slide = Slide()
        slide.layers = [TextLayer(text="Hola", role="title"), PhotoLayer(src="a.jpg")]
        restored = Slide.from_dict(slide.to_dict())
        self.assertEqual(restored.format, slide.format)
        self.assertEqual(len(restored.layers), 2)
        self.assertIsInstance(restored.layers[0], TextLayer)
        self.assertIsInstance(restored.layers[1], PhotoLayer)
        self.assertEqual(restored.layers[0].text, "Hola")
        self.assertEqual(restored.layers[1].src, "a.jpg")

    def test_round_trip_empty_layers(self):
        slide = Slide()
        restored = Slide.from_dict(slide.to_dict())
        self.assertEqual(restored.layers, [])


class TestProject(unittest.TestCase):
    def test_default_palette_matches_v2(self):
        project = Project()
        self.assertEqual(project.palette["verde"], [141, 194, 111])

    def test_round_trip_preserves_slides(self):
        project = Project(name="Mi Proyecto")
        slide = Slide()
        slide.layers = [TextLayer(text="Hola")]
        project.slides = [slide]
        restored = Project.from_dict(project.to_dict())
        self.assertEqual(restored.name, "Mi Proyecto")
        self.assertEqual(len(restored.slides), 1)
        self.assertEqual(len(restored.slides[0].layers), 1)
        self.assertEqual(restored.slides[0].layers[0].text, "Hola")

    def test_shared_defaults_to_empty_dict(self):
        project = Project()
        self.assertEqual(project.shared, {})


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Ejecutar el test y confirmar que falla**

Run: `python -m unittest tests.test_models_project -v`
Expected: FAIL / ERROR — `ImportError: cannot import name 'Slide' from 'dcpub.models'`

- [ ] **Step 3: Reemplazar `dcpub/models.py` con este contenido completo (Task 1 + Slide/Project)**

```python
"""Modelo de datos: Project, Slide, Layer y subclases, con (de)serialización a dict."""

from dataclasses import dataclass, field, asdict
import uuid

from .constants import VERDE, BLANCO, BOX_COLOR


def _short_id() -> str:
    return uuid.uuid4().hex[:8]


@dataclass
class Layer:
    id: str = field(default_factory=_short_id)
    name: str = ""
    type: str = "layer"
    visible: bool = True
    locked: bool = False
    z: int = 0
    x: float = 0.0
    y: float = 0.0
    w: float = 0.0
    h: float = 0.0
    rotation: float = 0.0
    opacity: float = 1.0

    def to_dict(self) -> dict:
        return asdict(self)


@dataclass
class PhotoLayer(Layer):
    type: str = "photo"
    src: str = ""
    fit: str = "cover"
    zoom: float = 1.0
    offset_x: float = 0.0
    offset_y: float = 0.0


@dataclass
class LogoLayer(Layer):
    type: str = "logo"
    src: str = ""
    keep_ratio: bool = True


@dataclass
class TextLayer(Layer):
    type: str = "text"
    text: str = ""
    role: str = "free"
    size: float = 0.05


@dataclass
class BoxLayer(Layer):
    type: str = "box"
    text: str = ""
    icon: str = "ninguno"
    size: float = 0.033


LAYER_CLASSES = {
    "photo": PhotoLayer,
    "logo": LogoLayer,
    "text": TextLayer,
    "box": BoxLayer,
}


def layer_from_dict(data: dict) -> Layer:
    """Reconstruye la subclase de Layer correcta según su campo "type"."""
    cls = LAYER_CLASSES[data["type"]]
    return cls(**data)


@dataclass
class Slide:
    format: dict = field(default_factory=lambda: {"name": "feed_4x5", "w": 1080, "h": 1350})
    layout_tag: str | None = None
    layers: list = field(default_factory=list)

    def to_dict(self) -> dict:
        return {
            "format": dict(self.format),
            "layout_tag": self.layout_tag,
            "layers": [layer.to_dict() for layer in self.layers],
        }

    @classmethod
    def from_dict(cls, data: dict) -> "Slide":
        return cls(
            format=dict(data["format"]),
            layout_tag=data.get("layout_tag"),
            layers=[layer_from_dict(l) for l in data.get("layers", [])],
        )


@dataclass
class Project:
    version: int = 1
    name: str = "Proyecto sin título"
    default_format: dict = field(default_factory=lambda: {"name": "feed_4x5", "w": 1080, "h": 1350})
    palette: dict = field(default_factory=lambda: {
        "verde": list(VERDE),
        "blanco": list(BLANCO),
        "box": list(BOX_COLOR),
    })
    shared: dict = field(default_factory=dict)
    slides: list = field(default_factory=list)

    def to_dict(self) -> dict:
        return {
            "version": self.version,
            "name": self.name,
            "default_format": dict(self.default_format),
            "palette": dict(self.palette),
            "shared": dict(self.shared),
            "slides": [slide.to_dict() for slide in self.slides],
        }

    @classmethod
    def from_dict(cls, data: dict) -> "Project":
        return cls(
            version=data.get("version", 1),
            name=data.get("name", "Proyecto sin título"),
            default_format=dict(data["default_format"]),
            palette=dict(data["palette"]),
            shared=dict(data.get("shared", {})),
            slides=[Slide.from_dict(s) for s in data.get("slides", [])],
        )
```

- [ ] **Step 4: Ejecutar el test y confirmar que pasa**

Run: `python -m unittest tests.test_models_project -v`
Expected: `OK` (6 tests pasan)

- [ ] **Step 5: Ejecutar también la suite de Task 1 para confirmar que no se rompió nada**

Run: `python -m unittest tests.test_models_layer -v`
Expected: `OK` (7 tests pasan)

- [ ] **Step 6: Commit**

```bash
git add tests/test_models_project.py dcpub/models.py
git commit -m "Agregar Slide y Project a dcpub/models.py con serializacion a dict"
```

---

### Task 3: Factory `crear_proyecto_por_defecto()`

**Files:**
- Create: `tests/test_models_factory.py`
- Modify: `dcpub/models.py` (agregar el import de `LOGO_FILE` y la función `crear_proyecto_por_defecto` al final)

**Interfaces:**
- Consumes: `dcpub.models.{Project, Slide, PhotoLayer, LogoLayer, TextLayer, BoxLayer}` (Tasks 1-2). `dcpub.constants.LOGO_FILE` (Tarea 1.1).
- Produces: `dcpub.models.crear_proyecto_por_defecto(photo_path: str = "") -> Project`. El `Project` devuelto tiene un `Slide` con 5 capas en este orden: `PhotoLayer` (foto de fondo), `LogoLayer` (logo), `TextLayer` (título, `role="title"`), `TextLayer` (subtítulo, `role="subtitle"`), `BoxLayer` (descripción). Los valores por defecto de cada capa son exactamente los de `DEFAULTS`/textos iniciales en `dcpub/app.py` (Tarea 1.1, ya commiteada): logo `x=0.40,y=0.022,size=0.20`; título `x=0.055,y=0.42,size=0.087`, texto `"Tu título aquí"`; subtítulo `x=0.50,y=0.55,size=0.050`, texto `"frase secundaria"`; descripción `x=0.05,y=0.808,size=0.033`, ícono `"planta"`, texto vacío.

- [ ] **Step 1: Escribir el test que falla — `tests/test_models_factory.py`**

```python
"""Tests de la factory crear_proyecto_por_defecto (dcpub.models)."""

import unittest

from dcpub.models import crear_proyecto_por_defecto


class TestFactory(unittest.TestCase):
    def setUp(self):
        self.project = crear_proyecto_por_defecto("foto.jpg")
        self.slide = self.project.slides[0]

    def test_creates_one_slide_with_five_layers(self):
        self.assertEqual(len(self.project.slides), 1)
        self.assertEqual(len(self.slide.layers), 5)

    def test_layer_order_matches_v2_draw_order(self):
        types = [layer.type for layer in self.slide.layers]
        self.assertEqual(types, ["photo", "logo", "text", "text", "box"])

    def test_defaults_match_v2_values(self):
        by_name = {layer.name: layer for layer in self.slide.layers}

        logo = by_name["Logo"]
        self.assertEqual((logo.x, logo.y, logo.w), (0.40, 0.022, 0.20))

        titulo = by_name["Título"]
        self.assertEqual((titulo.x, titulo.y, titulo.size), (0.055, 0.42, 0.087))
        self.assertEqual(titulo.role, "title")
        self.assertEqual(titulo.text, "Tu título aquí")

        subtitulo = by_name["Subtítulo"]
        self.assertEqual((subtitulo.x, subtitulo.y, subtitulo.size), (0.50, 0.55, 0.050))
        self.assertEqual(subtitulo.role, "subtitle")
        self.assertEqual(subtitulo.text, "frase secundaria")

        desc = by_name["Descripción"]
        self.assertEqual((desc.x, desc.y, desc.size), (0.05, 0.808, 0.033))
        self.assertEqual(desc.icon, "planta")
        self.assertEqual(desc.text, "")

    def test_photo_layer_uses_given_path(self):
        photo = self.slide.layers[0]
        self.assertEqual(photo.src, "foto.jpg")
        self.assertEqual((photo.x, photo.y, photo.w, photo.h), (0.0, 0.0, 1.0, 1.0))

    def test_logo_layer_points_to_logo_file(self):
        logo = self.slide.layers[1]
        self.assertTrue(logo.src.endswith("logo-sin-fondo.png"))

    def test_default_photo_path_is_empty_string(self):
        project = crear_proyecto_por_defecto()
        self.assertEqual(project.slides[0].layers[0].src, "")

    def test_round_trip_preserves_full_project(self):
        from dcpub.models import Project
        restored = Project.from_dict(self.project.to_dict())
        self.assertEqual(restored.to_dict(), self.project.to_dict())


if __name__ == "__main__":
    unittest.main()
```

- [ ] **Step 2: Ejecutar el test y confirmar que falla**

Run: `python -m unittest tests.test_models_factory -v`
Expected: FAIL / ERROR — `ImportError: cannot import name 'crear_proyecto_por_defecto' from 'dcpub.models'`

- [ ] **Step 3: Reemplazar `dcpub/models.py` con este contenido completo (Task 1 + Task 2 + factory)**

```python
"""Modelo de datos: Project, Slide, Layer y subclases, con (de)serialización a dict."""

from dataclasses import dataclass, field, asdict
import uuid

from .constants import VERDE, BLANCO, BOX_COLOR, LOGO_FILE


def _short_id() -> str:
    return uuid.uuid4().hex[:8]


@dataclass
class Layer:
    id: str = field(default_factory=_short_id)
    name: str = ""
    type: str = "layer"
    visible: bool = True
    locked: bool = False
    z: int = 0
    x: float = 0.0
    y: float = 0.0
    w: float = 0.0
    h: float = 0.0
    rotation: float = 0.0
    opacity: float = 1.0

    def to_dict(self) -> dict:
        return asdict(self)


@dataclass
class PhotoLayer(Layer):
    type: str = "photo"
    src: str = ""
    fit: str = "cover"
    zoom: float = 1.0
    offset_x: float = 0.0
    offset_y: float = 0.0


@dataclass
class LogoLayer(Layer):
    type: str = "logo"
    src: str = ""
    keep_ratio: bool = True


@dataclass
class TextLayer(Layer):
    type: str = "text"
    text: str = ""
    role: str = "free"
    size: float = 0.05


@dataclass
class BoxLayer(Layer):
    type: str = "box"
    text: str = ""
    icon: str = "ninguno"
    size: float = 0.033


LAYER_CLASSES = {
    "photo": PhotoLayer,
    "logo": LogoLayer,
    "text": TextLayer,
    "box": BoxLayer,
}


def layer_from_dict(data: dict) -> Layer:
    """Reconstruye la subclase de Layer correcta según su campo "type"."""
    cls = LAYER_CLASSES[data["type"]]
    return cls(**data)


@dataclass
class Slide:
    format: dict = field(default_factory=lambda: {"name": "feed_4x5", "w": 1080, "h": 1350})
    layout_tag: str | None = None
    layers: list = field(default_factory=list)

    def to_dict(self) -> dict:
        return {
            "format": dict(self.format),
            "layout_tag": self.layout_tag,
            "layers": [layer.to_dict() for layer in self.layers],
        }

    @classmethod
    def from_dict(cls, data: dict) -> "Slide":
        return cls(
            format=dict(data["format"]),
            layout_tag=data.get("layout_tag"),
            layers=[layer_from_dict(l) for l in data.get("layers", [])],
        )


@dataclass
class Project:
    version: int = 1
    name: str = "Proyecto sin título"
    default_format: dict = field(default_factory=lambda: {"name": "feed_4x5", "w": 1080, "h": 1350})
    palette: dict = field(default_factory=lambda: {
        "verde": list(VERDE),
        "blanco": list(BLANCO),
        "box": list(BOX_COLOR),
    })
    shared: dict = field(default_factory=dict)
    slides: list = field(default_factory=list)

    def to_dict(self) -> dict:
        return {
            "version": self.version,
            "name": self.name,
            "default_format": dict(self.default_format),
            "palette": dict(self.palette),
            "shared": dict(self.shared),
            "slides": [slide.to_dict() for slide in self.slides],
        }

    @classmethod
    def from_dict(cls, data: dict) -> "Project":
        return cls(
            version=data.get("version", 1),
            name=data.get("name", "Proyecto sin título"),
            default_format=dict(data["default_format"]),
            palette=dict(data["palette"]),
            shared=dict(data.get("shared", {})),
            slides=[Slide.from_dict(s) for s in data.get("slides", [])],
        )


def crear_proyecto_por_defecto(photo_path: str = "") -> Project:
    """Crea el proyecto por defecto (foto + logo + título + subtítulo + caja) con los valores de v2.0."""
    slide = Slide()
    slide.layers = [
        PhotoLayer(name="Foto", z=0, x=0.0, y=0.0, w=1.0, h=1.0, src=photo_path),
        LogoLayer(name="Logo", z=1, x=0.40, y=0.022, w=0.20, h=0.20, src=str(LOGO_FILE)),
        TextLayer(name="Título", z=2, x=0.055, y=0.42, role="title", size=0.087,
                  text="Tu título aquí"),
        TextLayer(name="Subtítulo", z=3, x=0.50, y=0.55, role="subtitle", size=0.050,
                  text="frase secundaria"),
        BoxLayer(name="Descripción", z=4, x=0.05, y=0.808, size=0.033,
                 text="", icon="planta"),
    ]
    project = Project()
    project.slides = [slide]
    return project
```

- [ ] **Step 4: Ejecutar el test y confirmar que pasa**

Run: `python -m unittest tests.test_models_factory -v`
Expected: `OK` (7 tests pasan)

- [ ] **Step 5: Ejecutar toda la suite de tests del proyecto para confirmar que nada se rompió**

Run: `python -m unittest discover -s tests -v`
Expected: `OK` — deben pasar los 8 tests de Tarea 1.1 (`test_fonts.py`, `test_render.py`) más los de esta tarea (`test_models_layer.py`, `test_models_project.py`, `test_models_factory.py`).

- [ ] **Step 6: Commit**

```bash
git add tests/test_models_factory.py dcpub/models.py
git commit -m "Agregar factory crear_proyecto_por_defecto con los valores de v2.0"
```

---

## Self-Review Notes

- **Cobertura de la Tarea 1.2 del plan maestro:** `models.py` con `Project`, `Slide`, `Layer` y subclases como dataclasses (Task 1-2), `to_dict`/`from_dict` (Task 1-2), factory que crea el proyecto por defecto con los valores de v2.0 (Task 3). Todo lo pedido textualmente por CLAUDE.md sección 7 "1.2 Modelo de datos" está cubierto.
- **Sin placeholders:** cada paso trae el contenido completo y ejecutable de `dcpub/models.py` en su estado acumulado (Task 2 y 3 repiten el contenido de tasks anteriores porque están modificando el mismo archivo — evita instrucciones ambiguas de "insertar después de la línea X").
- **Consistencia de tipos:** `layer_from_dict(data: dict) -> Layer` se usa igual en Task 1 (tests directos) y Task 2 (`Slide.from_dict`); `Slide.from_dict`/`to_dict` se usan igual en Task 2 (tests directos) y Task 3 (`Project.to_dict`/`from_dict`); la factory devuelve exactamente los tipos y campos que sus propios tests (Task 3) validan.
- **Alcance deliberadamente acotado:** no se agregan `LineLayer`/`CTALayer`/`DotsLayer` (Fase 4), ni `adjust{}`/`overlay{}` en `PhotoLayer` (Fase 3), ni la paleta nueva de marca (Fase 5), ni wiring hacia `render.py`/`app.py` (tareas 1.3+). Cada omisión está anotada en Global Constraints con la fase donde corresponde, para que no se pierda de vista.
- **Riesgo conocido:** el modelo todavía no está conectado a la GUI — `dcpub/app.py` sigue usando su propio `self.el` (dict) y `_build_layers()` en vez de `Project`/`Slide`/`Layer`. Confirmado como aceptable por el usuario durante el diseño: la integración llega en tareas posteriores cuando `CanvasEditor` (1.4) necesite seleccionar/mover capas reales por `id`.
